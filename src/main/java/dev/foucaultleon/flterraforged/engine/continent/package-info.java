/**
 * Minecraft-independent continent partitioning and coastal-shape generation.
 *
 * <p>Continent implementations populate engine {@code Cell} signals and expose deterministic
 * samples without depending on biome, registry, river-map or loader types.</p>
 */
package dev.foucaultleon.flterraforged.engine.continent;
