/**
 * Deterministic hydraulic and thermal erosion stages applied after base terrain shaping.
 *
 * <p>The package operates entirely on engine height fields and caller-owned cells. It contains no
 * Minecraft world, chunk, block, biome, codec or loader objects.</p>
 */
package dev.foucaultleon.flterraforged.engine.erosion;
